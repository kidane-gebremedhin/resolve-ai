// Deprecated — superseded by `WidgetRoot.tsx` + the 8-state machine in
// `lib/state-machine.ts`. Kept as an empty file so any straggling imports
// throw a clear "no exports" error instead of bringing back the old UI.
//
// Safe to delete in a follow-up cleanup once we've verified no embed/CDN
// references the legacy component name.
export {};
