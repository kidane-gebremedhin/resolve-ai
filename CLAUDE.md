@AGENTS.md

### Always ask clarifying question when unclear befor implemention

### Unless explicitly stated, always use the current project directory as a base file path

### Always update existing specs, plans, etc.. to accomodate the new chages implemented, so that changes persist on brand new code regeneration from specs, plans, etc...

### Always create new CHANGELOGS_[1-BASED INDEX NUMBER].md outlining only the new chages implemented (Don't outline changes from previous sessions)

### When creating new environment variable, make sure it is added to .env.example
